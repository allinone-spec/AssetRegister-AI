  // const fetchSourceData = async () => {
    //     setLoading(true);
    //     try {
    //         if (!file?.viewName) {
    //             throw new Error("Job name is missing.");
    //         }

    //         const response = await getRequest(`/table/getAC/${encodeURIComponent(file?.jobName)}/data`);
    //         if (response?.status === 200) {
    //             setSourceData(response.data || []);
    //             console.log('Data of All SourceData:', response?.data);
    //         }
    //     } catch (error) {
    //         console.error("Error fetching data:", error);
    //         setSourceData([]);
    //     } finally {
    //         setLoading(false);
    //     }
    // };


       {/* {selectedColumns?.map((formattedKey, index) => (
                                        <DraggableColumn key={index} index={index} column={formattedKey} />

                                    ))} */}

                                    {/* {fieldsArray?.map((formattedKey, index) => (
                                        <DraggableColumn key={index} index={index} column={formattedKey} />

                                    ))} */}