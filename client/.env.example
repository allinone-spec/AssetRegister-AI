# AI service base URLs (no trailing slash). Used by src/Service/ai.service.js
# Development (npm run dev) uses LOCAL; production build uses SERVER.
# Or set only VITE_AI_BASE_URL to use the same URL for both.

VITE_AI_BASE_URL_LOCAL=http://localhost:9090/api/ai
VITE_AI_BASE_URL_SERVER=http://20.197.51.21:9090/api/ai

# Optional: single URL for both modes
# VITE_AI_BASE_URL=http://20.197.51.21:9090/api/ai
